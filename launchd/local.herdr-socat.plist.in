<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!-- Generated by `make install` from launchd/local.herdr-socat.plist.in.
     Edit the template in the repo, not the installed copy. -->
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>@LABEL@</string>

    <!-- Bridges the herdr Unix socket to TCP so containers (OrbStack) can reach it
         via host.docker.internal:@PORT@. bind=127.0.0.1 keeps it off the LAN; OrbStack
         still forwards host.docker.internal to the host loopback. -->
    <key>ProgramArguments</key>
    <array>
        <string>@SOCAT@</string>
        <string>TCP-LISTEN:@PORT@,bind=127.0.0.1,reuseaddr,fork</string>
        <string>UNIX-CONNECT:@SOCKET@</string>
    </array>

    <key>RunAtLoad</key>
    <true/>
    <key>KeepAlive</key>
    <true/>
    <key>ThrottleInterval</key>
    <integer>10</integer>

    <key>StandardOutPath</key>
    <string>@LOG@</string>
    <key>StandardErrorPath</key>
    <string>@LOG@</string>
</dict>
</plist>
